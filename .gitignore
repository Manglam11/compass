# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Golden data (may contain real resumes)
data/golden/*
!data/golden/.gitkeep

# Resume files (never committed — labels reference them by hash only)
resumes/*
!resumes/PROVENANCE.csv
resumes_clean/
resumes_stripped/
corpus/

# Explicit name lists for redaction (PII — never committed)
names.txt
*.names.txt

# Generated documents
*.pdf
*.docx
resumes_stripped_v2/

# Rung 3 eval harness output (resume ids/skill ids/timings only, but never committed)
results/
